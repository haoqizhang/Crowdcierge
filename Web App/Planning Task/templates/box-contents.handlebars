<div id='boxTop'>
    <h1> {{title}}
        <span style='float:right;'>
            {{#each controlButtons}}
            <button class='addit' onclick='{{callback}}()'> 
                {{text}}
            </button>
            {{/each}}
        </span>
    </h1>

    <div id='boxContent'>
    </div>
</div>